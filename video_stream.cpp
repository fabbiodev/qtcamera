#include "video_stream.h"

// Пока заготовка: сюда позже добавится сборка и обработка видеопотока.
void videoStreamSubmitRawFrame(const RawFrame &frame)
{
    (void)frame;                                        // кадр пока не используется
}
